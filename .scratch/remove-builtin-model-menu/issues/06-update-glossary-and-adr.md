# 06 — 更新术语表与 ADR

**What to build:** 领域文档与项目记录与新的模型语义一致：可用模型只来自配置，解析只认配置，不再存在内置模型菜单。

**Blocked by:** 05 — 删除内置目录与推断死代码

**Status:** ready-for-agent

- [ ] `CONTEXT.md` 中的模型相关词条与最终行为一致。
- [ ] 新增或更新 ADR，记录移除内置模型菜单、模型解析只认配置的决策。
- [ ] 文档不再把内置预设目录描述为 `/models` 的内容来源。
